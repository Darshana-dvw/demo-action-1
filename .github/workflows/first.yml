name: My Awesome App
on: push
jobs:
    first-job:
        runs-on: ubuntu-latest
        steps: 
            - name:  create a folder
              run: mkdir new-folder
            - name: create a file
              run: echo "hello everyone">file.txt
            - name: Read file
              run: file.txt